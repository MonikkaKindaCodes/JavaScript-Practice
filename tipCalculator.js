//Tip Calculator Exercise

const bill = 275;
let tip =.15 * bill;
total = tip + bill
bill >= 20 && bill <= 300 ? tip : tip = .2 * bill, total = tip + bill;
console.log(`Your bill was ${bill}, the tip was ${tip}, and the total value is ${total}`);